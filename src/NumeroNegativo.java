public class NumeroNegativo extends Exception {
    /**
     * Creo una excepción extendida de las propias de java a la que le llega por parámetro un mensaje
     * @param message
     */
    public NumeroNegativo(String message) {
        super(message);
    }
}
